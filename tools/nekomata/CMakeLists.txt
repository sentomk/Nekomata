add_executable(nekomata main.cpp)
target_link_libraries(nekomata PRIVATE nekomata::neko)
if(TARGET neko_backend_dwarf)
  target_sources(nekomata PRIVATE inspect.cpp)
  target_include_directories(nekomata PRIVATE "${PROJECT_SOURCE_DIR}/src/platforms/elf")
  target_link_libraries(nekomata PRIVATE neko_function_index)
  target_compile_definitions(nekomata PRIVATE NEKOMATA_HAS_DWARF_INSPECTION=1)
endif()
nekomata_apply_warnings(nekomata)
nekomata_apply_sanitizers(nekomata)
nekomata_apply_clang_tidy(nekomata)
